# Cargo generated compiled files and executables
debug/
target/

# Rust best practice: Include Cargo.lock for executables (ensuring reproducible
# builds), exclude for libraries (allowing flexibility for users).
Cargo.lock

# Backup files generated by rustfmt
**/*.rs.bk
